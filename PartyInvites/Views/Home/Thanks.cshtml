@model GuestResponse
@{
    ViewData["Title"] = "Thanks for your answer";
}
<!DOCTYPE html>
<html>
<head>
    <meta name=”viewport” content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <p>
        <h1>Thank you, @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @:It's great that you are coming!
        }
        else
        {
            @: Sorry to hear that you can't
        }
    </p>
        <p>Click <a asp-action="ListResponses">here</a> to see who is comming</p>
</body>
</html>

